# PostgreSQL connection string (required)
DATABASE_URL=postgresql://user:password@localhost:5432/real_time_poll

# Port (optional; default 3001)
PORT=3001

# Frontend origin for CORS and shareable links (set in production)
# FRONTEND_URL=https://your-app.vercel.app
